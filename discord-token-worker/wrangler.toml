name = "flippinghusks-token-proxy"
main = "worker.js"
compatibility_date = "2024-05-01"

# Secrets (DISCORD_CLIENT_ID, DISCORD_CLIENT_SECRET, PROXY_SECRET) are NOT set here —
# set them with `wrangler secret put <NAME>` (or in the dashboard) so they stay out
# of source control. See README.md.
